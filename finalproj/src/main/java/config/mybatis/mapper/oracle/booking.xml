<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.onair.proj.booking.controller.BookingDAO">
	<select id="ticketDetail" parameterType="int" resultType="ticketviewVo">
	select * from ticketView
	where r_no=#{r_no}
	</select>
</mapper>