<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.onair.proj.file.model.FileDAO">
	<insert id="insertFile" parameterType="fileVo">
		<selectKey resultType="int" keyProperty="fNo" order="BEFORE">
			select seq_FILES.nextval from dual
		</selectKey>
		
		insert into files(f_no, f_name,f_filesize,f_originname,b_no)
		values(#{fNo},#{fName},#{fFilesize},#{fOriginname},#{bNo)
	</insert>
</mapper>