package com.onair.proj.kakao;

import java.net.HttpURLConnection;
import java.net.MalformedURLException;
import java.net.URL;

import org.springframework.stereotype.Service;

@Service
public class KakaoServiceImpl {
	
	/*
	 * public String getAccessToken(String authorize_code) { String access_Token="";
	 * String refresh_Token=""; String reqURL="https://kauth.kakao.com/oauth/token";
	 * 
	 * try { URL url= new URL(reqURL); HttpURLConnection con= (HttpURLConnection)
	 * url.openConnection();
	 * 
	 * con.setRequestMethod("POST"); con.setDoOutput(true);
	 * 
	 * } catch (MalformedURLException e) { e.printStackTrace(); }
	 * 
	 * 
	 * }
	 */

}
